---
- name: Configure vulkan tweaks
  ansible.builtin.copy:
    content: |
      export ANV_DEBUG=video-decode,video-encode
    dest: /etc/profile.d/vulkan.sh
    mode: "0644"
- name: Set timezone
  community.general.timezone:
    name: "Europe/Prague"
- name: TCP congestion control algorithm
  ansible.builtin.copy:
    content: net.ipv4.tcp_congestion_control = bbr
    dest: /etc/sysctl.d/tcp_congestion_control.conf
    mode: "0644"
- name: Enable IPv6 ndisc notify
  ansible.builtin.copy:
    content: |
      net.ipv6.conf.all.ndisc_notify = 1
      net.ipv6.conf.default.ndisc_notify = 1
    dest: /etc/sysctl.d/ndisc_notify.conf
    mode: "0644"
- name: Use lz4 for zram
  community.general.ini_file:
    path: /usr/lib/systemd/zram-generator.conf
    section: zram0
    option: compression-algorithm
    value: lz4
    mode: "0644"
- name: Set zram size
  community.general.ini_file:
    path: /usr/lib/systemd/zram-generator.conf
    section: zram0
    option: zram-size
    value: ram
    mode: "0644"
- name: Set swappiness
  ansible.builtin.copy:
    content: vm.swappiness = 150
    dest: /etc/sysctl.d/swappiness.conf
    mode: "0644"
- name: Do not enable BT at boot
  community.general.ini_file:
    path: /etc/bluetooth/main.conf
    section: Policy
    option: AutoEnable
    value: "false"
    no_extra_spaces: true
    mode: "0644"
- name: Configure disk I/O scheduler
  ansible.builtin.copy:
    content: |
      ACTION=="add|change", SUBSYSTEM=="block", KERNEL=="nvme[0-9]*n[0-9]*", ATTR{queue/scheduler}="kyber"
      ACTION=="add|change", SUBSYSTEM=="block", KERNEL=="sd[a-z]|mmcblk[0-9]*", ATTR{queue/rotational}=="0", ATTR{queue/scheduler}="mq-deadline"
      ACTION=="add|change", SUBSYSTEM=="block", KERNEL=="sd[a-z]", ATTR{queue/rotational}=="1", ATTR{queue/scheduler}="bfq"
    dest: /etc/udev/rules.d/60-iosched.rules
    mode: "0644"
- name: Create dnf5daemon-server service override directory
  ansible.builtin.file:
    path: /etc/systemd/system/dnf5daemon-server.service.d
    state: directory
    mode: "0755"
- name: Set dnf5daemon-server stop timeout
  ansible.builtin.copy:
    content: |
      [Service]
      TimeoutStopSec=30
    dest: /etc/systemd/system/dnf5daemon-server.service.d/override.conf
    mode: "0644"
