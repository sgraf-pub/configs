---
- name: Remove writeback setting
  ansible.builtin.file:
    path: /etc/sysctl.d/dirty.conf
    state: absent
- name: Remove WoL setting
  ansible.builtin.file:
    path: /etc/udev/rules.d/wol.rules
    state: absent
- name: Remove legacy net powersaving
  ansible.builtin.file:
    path: /etc/udev/rules.d/net_power_save.rules
    state: absent
- name: Remove legacy usb rule
  ansible.builtin.file:
    path: /etc/udev/rules.d/usb_power_save.rules
    state: absent
- name: Remove legacy pci rules
  ansible.builtin.file:
    path: /etc/udev/rules.d/pci_pm.rules
    state: absent
- name: Install powertop service
  ansible.builtin.copy:
    content: |
      [Unit]
      Description=Powertop tunings

      [Service]
      Type=oneshot
      RemainAfterExit=yes
      ExecStart=/usr/bin/powertop --auto-tune
      ExecStartPost=/bin/sh -c 'find /sys/bus/usb/drivers/usbhid -regex ".*/[0-9:.-]+" | sed "s|.*/||;s|:.*||" | sort -u | xargs -rI{} sh -c "echo on > /sys/bus/usb/devices/{}/power/control"'

      [Install]
      WantedBy=multi-user.target sleep.target
    dest: /etc/systemd/system/powertop.service
    mode: "0644"
- name: Enable powertop service
  ansible.builtin.systemd_service:
    name: powertop.service
    enabled: true
    daemon_reload: true
- name: Install i915 power settings
  ansible.builtin.copy:
    content: "options i915 enable_dc=4 disable_power_well=1\n"
    dest: /etc/modprobe.d/i915.conf
    mode: "0644"
  when: ansible_facts.architecture == 'x86_64'
- name: Install iwlwifi power settings
  ansible.builtin.copy:
    content: "options iwlwifi power_save=1 power_level=3\n"
    dest: /etc/modprobe.d/iwlwifi.conf
    mode: "0644"
  when: ansible_facts.architecture == 'x86_64'
- name: Install PCIe ASPM powersave policy
  ansible.builtin.copy:
    content: "w /sys/module/pcie_aspm/parameters/policy - - - - powersave\n"
    dest: /etc/tmpfiles.d/aspm-powersave.conf
    mode: "0644"
